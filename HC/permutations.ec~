pragma Goals:printall.
require import AllCore DBool Bool List Distr Int IntDiv Aux FSet.

type permutation =  int -> int.

print List.

op cr_perm (l1 l2 : int list) (x : int) : int 
  = find (pred1 (oget (onth l1 x))) l2.


lemma zz l1 l2 : uniq l1 => uniq l2 
 => perm_eq l1 l2 => exi  
